# Vite dev server の API プロキシ先（本番では未使用）
API_URL=http://server:4000

# 本番 web から server への直接アクセス URL（同一オリジン配信なら未設定で OK）
# VITE_API_URL=https://api.example.com/seseraki

# swars 棋譜取得対象のユーザー ID
VITE_SWARS_USER_ID=YourSwarsUserId
